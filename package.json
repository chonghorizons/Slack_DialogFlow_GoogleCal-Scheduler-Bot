{
  "name": "schedulerbot",
  "version": "1.0.0",
  "description": "Slack+GoogleCalendar+DialogFlow =  natural-language scheduler bot",
  "main": "server.js",
  "scripts": {
    "babel": "mkdir -p build && babel server.js -o build/main.bundle.js",
    "lint": "eslint --fix *.js",
    "start": "babel-node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kevin Nguyen and Howard Chong",
  "license": "SEE LICENSE IN README",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "eslint": "^5.3.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.14.0"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.3",
    "googleapis": "^27.0.0"
  }
}
